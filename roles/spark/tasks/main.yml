---
- name: Create Spark directory
  sudo: true
  file:
    path: "{{ spark_client_dir }}"
    state: directory
  tags:
    - spark

- name: Download Spark binary
  sudo: true 
  get_url: 
    url: "http://mirror.ox.ac.uk/sites/rsync.apache.org/spark/spark-{{ spark_version }}/spark-{{ spark_version }}-bin-hadoop2.6.tgz"
    dest: "{{ spark_client_dir }}/spark-{{ spark_version }}-bin-hadoop2.6.tgz"
  register: spark_downloaded
  tags:
    - spark 

- name: Create Spark user home directory on HDFS
  run_once: true
  sudo_user: hdfs
  sudo: true
  shell: source /etc/profile.d/hadoop.sh; if ! hdfs dfs -test -d /user/{{ spark_user }}; then hdfs dfs -mkdir -p /user/{{ spark_user }}; fi; hdfs dfs -chown {{ spark_user }} /user/{{ spark_user }}
  tags:
    - spark

# for future use

#- name: Create Spark directory on HDFS
#  run_once: true
#  sudo_user: hdfs
#  sudo: true
#  shell: source /etc/profile.d/hadoop.sh; if ! hdfs dfs -test -d {{ spark_install_dir }}; then hdfs dfs -mkdir -p {{ spark_install_dir }}; fi; hdfs dfs -chown {{ spark_user }} {{ spark_install_dir }}
#  tags:
#    - spark

#- name: Copy Spark binary to HDFS
#  run_once: true
#  shell: source /etc/profile.d/hadoop.sh; if ! hdfs dfs -test -e {{ spark_install_dir }}/spark-{{ spark_version }}-bin-hadoop2.6.tgz; then hdfs dfs -put {{ spark_client_dir }}/spark-{{ spark_version }}-bin-hadoop2.6.tgz {{ spark_install_dir }}/spark-{{ spark_version }}-bin-hadoop2.6.tgz; fi; hdfs dfs -chown {{ spark_user }} {{ spark_install_dir }}/spark-{{ spark_version }}-bin-hadoop2.6.tgz
#  when: spark_downloaded|changed
#  tags:
#    - spark         

- include: client.yml
